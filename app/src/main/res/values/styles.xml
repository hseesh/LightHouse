<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="mycheckbox" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/style_add</item>
        <item name="android:background">@drawable/style_add</item>
    </style>

    <style name="mycheckbox2" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/style_love</item>
        <item name="android:background">@drawable/style_love</item>
    </style>

    <declare-styleable name="StatisticsWeek">
       <attr name="isMonth" format="boolean">

        </attr>
    </declare-styleable>

    <style name="MyDialogStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="MyEditText" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@android:color/darker_gray</item>
        <item name="colorControlActivated">@color/color_blue_light</item>
    </style>


</resources>
